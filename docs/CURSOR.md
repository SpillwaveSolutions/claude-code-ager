# Cursor

Cursor is a Core Maintainer of [Agent Plugins 1.0](https://agent-plugins.org/specification).
It loads this repo as a universal plugin: `plugin.json` + `skills/`.

```
/plugin install claude-code-ager
```

or copy `skills/` into `.cursor/skills/`.

Identity: `cursor/claude-code-ager`. Author with `okf-agent-graph`. Prefer `scripts/emit.py`.
Never document a private remote.
