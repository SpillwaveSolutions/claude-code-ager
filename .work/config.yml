# WikiTicket SDD settings. Policy lives in AGENTS.md / CLAUDE.md.
version: 1

project:
  key: CCAGER
  name: "Claude Code AGER translator"

ticketing:
  system: github
  project: SpillwaveSolutions/claude-code-ager

wiki:
  system: github-wiki
  root_url: https://github.com/SpillwaveSolutions/claude-code-ager/wiki

paths:
  plans: docs/plans
  status: docs/status
  roadmap: docs/roadmap.md

status:
  default_kind: daily
  week_starts: monday
  publish: true

sync:
  active_window_days: 14
  conflict_policy: report
  push_on_capture: true

features:
  auto_merge_on_green: true

classifier:
  enabled: false
  min_confidence: 0.7
  debounce: stop

installed: 0.24.4
